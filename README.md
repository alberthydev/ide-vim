# NVIM IDE

# Here I have all my plugins and keybinds set for my personal use

> [!NOTE]
> For installation, use this `git clone git@github.com:alberthydev/ide-vim.git "${XDG_CONFIG_HOME:-$HOME/.config}"/nvim`
